# 로드맵 · 알려진 제약

이 문서는 **아직 반영되지 않은 개선 항목**과, 사용자가 알아 두면 좋은 현재 동작의
한계를 적어 둡니다. 완료된 변경 내역은 프로그램 안의 릴리즈 노트를 참고하세요.

---

## 사용자가 알아 두면 좋은 것

### 데이터 암호화가 적용되는 범위

암호화를 켜면 다음이 암호화되어 저장됩니다.

- 학생 이름
- 활동 기록 내용
- 기록 히스토리(이전 버전) 내용
- 기록 버전을 저장할 때 남기는 **메모**
- 스냅샷을 만들 때 남기는 **메모**

반면 **아래는 암호화되지 않습니다.** 이 칸들에 학생 이름이나 민감한 내용을 적지
않는 것을 권합니다.

- 텍스트 치환 규칙의 찾을 말·바꿀 말 (예: `홍길동` → `본인` 같은 규칙)
- 유의어 점검에 등록한 단어와 묶음 이름
- 활동 이름, 활동 영역 이름
- 기록이 마지막으로 바뀐 시각 등 날짜·시각 정보

암호화를 켠 뒤에 만들어진 백업 파일도 본 파일과 같은 상태로 저장됩니다. 다만
**암호화를 켜기 전이나 메모 암호화가 적용되기 전에 만들어진 백업 파일**에는 평문이
남아 있습니다.

숨겨지는 것은 **내용**이고, 그 내용이 얼마나 긴지까지 숨기지는 않습니다. 예를 들어
기록 히스토리에는 프로그램이 자동으로 붙이는 짧은 라벨("가져오기 전", "치환 적용 전"
등)이 함께 저장되는데, 암호화해도 길이가 서로 달라 어떤 작업이었는지는 구분될 수
있습니다. 학생 이름이나 기록 내용이 드러나지는 않습니다.

### 메모 암호화가 적용되는 첫 열기는 로컬 디스크에서 하시길 권합니다

이 변환은 파일 전체를 다시 씁니다. 그동안 같은 폴더에 SQLite가 만드는 임시 파일이
생기는데, 그 안에는 **아직 암호화되지 않은 메모가 통째로** 들어 있습니다. 변환이
끝나면 사라지지만, 파일을 OneDrive·구글 드라이브 같은 동기화 폴더에 두셨다면 그
사이에 클라우드로 올라갈 수 있습니다.

변환하는 그 한 번만 파일을 로컬 디스크(예: 바탕화면)로 옮겨 열고, 끝난 뒤 원래
자리로 되돌리시면 안전합니다. 이미 동기화 폴더에서 변환하셨다면, 클라우드의 이전
버전 기록도 함께 정리해 주세요.

### 저장이 느린 환경에서는 잠시 멈출 수 있습니다

기록을 저장하는 동안에는 프로그램이 잠깐 반응하지 않습니다. 일반적인 로컬 디스크에서는
저장이 순식간에 끝나 알아채기 어렵지만, **파일을 학교 공유 폴더·네트워크 드라이브·
클라우드 동기화 폴더(OneDrive 등)에 두었거나 백신이 파일을 검사 중이면** 저장이 몇 초
걸릴 수 있고, 그동안 화면이 멈춘 것처럼 보입니다. **그 사이에 친 글자는 입력되지 않습니다.**

메모 암호화가 적용되는 **첫 열기**도 같은 이유로 평소보다 오래 걸립니다. 그 한 번은
파일 전체를 두 번 다시 씁니다 — 되돌릴 수 있도록 사본을 만들고, 변환이 끝난 뒤 옛
자리에 남은 평문 흔적을 지웁니다. 기록이 많을수록 오래 걸리며, **다음 열기부터는
평소 속도로 돌아갑니다.**

가능하면 **파일을 PC 내부 디스크에 두고 사용**하시고, 작업이 끝난 뒤 공유 폴더로
복사하시는 편이 안전합니다.

프로그램이 저장을 처리하는 방식 자체를 바꿔야 해결되는 문제라 예정 작업으로 두었습니다.

### 자동 백업 파일은 자동으로 지워지지 않습니다

파일을 열 때마다 같은 폴더에 `*.db.backup` 파일이 만들어집니다. 프로그램은 이
파일들을 스캔하지도, 지우지도 않습니다. 파일 이름만으로는 프로그램이 만든 것인지
사용자가 보관 중인 것인지 구분할 수 없기 때문입니다. **주기적으로 직접 정리해
주세요.**

암호화를 켜기 전에 만들어진 백업은 암호화되지 않은 사본입니다. 암호화를 켠 뒤에도
그 파일들은 그대로 남아 있으므로 함께 정리하시는 것이 좋습니다.

---

## 예정 작업

### 복호화 실패 기록의 복구 경로 (F2)

지금은 기록 한 건이라도 복호화되지 않으면 목록 조회 전체가 중단되고, 암호화 해제도
같은 지점에서 막혀 나머지를 건져낼 방법이 없다. 어떤 기록이 문제인지도 알려주지 않고,
에러 메시지도 "비밀번호가 올바르지 않습니다"로 오도한다.

정상적인 사용에서는 발생하지 않고, 파일을 다른 도구로 직접 수정했거나 아주 오래된
버전으로 열어 쓴 경우에 생길 수 있다. 필요한 것은 두 가지다.

- 문제가 된 기록을 특정해서 알려주기
- 나머지 기록만이라도 열거나 내보낼 수 있는 경로

메모 암호화 소급 적용(구 F1)의 선행 조건으로 잡아 두었으나, 그 작업은 이 항목 없이
끝났다(v2 마이그레이션). 소급 변환이 하는 일은 **암호화**뿐이라 사용자 데이터를
복호화하지 않고, 따라서 "어느 행이 복호화에 실패했는지"를 말해야 하는 상황이 변환
도중에는 생기지 않는다. 변환 단계가 값을 복호화해 보는 곳은 이중 암호화를 막는 멱등
검사 하나인데, 거기서는 실패가 곧 "아직 평문"이라는 정상 신호다.

남은 것은 조회·암호화 해제 경로의 문제 그대로다. 수정하려면 조회의 의미를 바꾸는 설계
변경이 필요하다.

### 저장 중 화면 멈춤 해소

원인은 확인됐다. **Tauri 커맨드가 `async`가 아니면 기본 `execution_context`가
`Blocking`이라 메인 스레드에서 실행된다.** 이 프로젝트는 `commands/config.rs`의 넷을
빼고 전부 sync다. 그래서 저장이 오래 걸리면 그동안 화면 전체가 멈추고 그 사이의 키
입력이 사라진다.

암호화를 켜다가 비정상 종료된 파일을 다시 열 때도 같은 일이 생긴다. 밀린 정리(VACUUM)를
마저 하느라 파일 크기에 비례해 멈춘다. 그 파일을 한 번 열고 나면 재발하지 않는다.

처리를 별도 스레드로 옮기면 해결되지만, `Mutex`로 공유하는 DB 연결 하나를 어떻게 나눌지
까지 함께 손봐야 해서 범위가 크다. 저장 자체는 정상 완료되고 데이터가 깨지지는 않으므로
뒤로 두었다.

### 암호화 키의 메모리 수명 (F5)

잠금 해제 후 키는 프로그램을 닫거나 다른 파일을 열 때까지 메모리에 남아 있습니다.
별도의 "잠그기" 기능은 아직 없습니다. 또한 키가 값으로 복사되는 구조라, 메모리에서
지우는 처리가 완전하지 않습니다.

이 항목은 **프로그램이 실행 중인 PC의 메모리를 직접 들여다볼 수 있는 상황**을
전제로 하므로, 파일이 복사·유출되는 상황을 막는 것을 목표로 하는 현재 설계에서는
우선순위가 낮습니다.

---

## 알려진 미해결 (조치 불가·증거 대기)

- **`xlsx` high 2건**(Prototype Pollution·ReDoS) — npm 배포가 중단돼 수정 불가.
  SheetJS는 한셀 파일 폴백이라 제거도 못 한다. CDN 버전으로 옮기는 안이 열려 있다.
- **`glib` medium 1건**(`VariantStrIter` unsoundness) — tauri의 Linux GTK 백엔드 경유
  전이 의존성이라 Windows·macOS 빌드에는 컴파일조차 되지 않는다. 앱이 직접 쓰지도 않는다.
  tauri가 gtk 스택을 올려야 사라진다.
- **`test_enable_disable_reenable_works` 플레이크** — 로컬 Windows 전체 병렬 실행에서
  1회 실패 후 재현되지 않았다. 백업 삭제 실패 가설은 그 테스트가 복사하는 파일이
  0바이트라 성립하지 않는다. **증거 없이 재시도 로직을 넣지 말 것.** 오류 메시지에 OS
  원문이 포함되므로 재발하면 그때 판단한다.

---

## 감사·검증 방식에 대한 판단

코드베이스 전반의 **정적 재감사는 지금 필요하지 않다.** 영역별(암호화·프론트엔드·
DB) 정적 감사가 이미 돌았고, 위 항목들이 그 산출물이다. 같은 코드에 다시 돌리면
같은 목록이 나온다.

대신 두 가지가 공백이다.

- **정적 감사가 구조적으로 못 잡는 영역이 있다.** 종료 훅(`onCloseRequested`) 작업에서
  빌드와 테스트가 전부 통과한 상태로 앱이 닫히지 않았고, 정적 감사 세 번이 모두 놓쳤다.
  `capabilities/*.json`을 아무도 보지 않았기 때문이다. Tauri 권한과 비동기 생명주기는
  런타임에만 드러난다. 여기 필요한 것은 감사가 아니라 **손으로 도는 확인 목록**이다.
- **사용자 파일의 기존 데이터를 제자리에서 다시 쓰는 작업은 감사 대상이 맞다.** 이
  프로젝트에서 위험도가 가장 높은 축이다. 감사 시점은 **구현 후 diff**다. 메모 암호화
  소급 적용(스키마 v2)이 그 사례였고, 감사가 잡아낸 것은 암호화 로직 자체가 아니라
  그 **주변**이었다 — 마이그레이션 직전에 앱이 만드는 백업이 평문 사본으로 남는 문제,
  그리고 방어 장치 두 겹이 서로를 가려 어느 한쪽을 지워도 테스트가 통과하던 문제.
  다음에도 트랜잭션 안쪽만 보지 말 것.

---

## 기능 후보

### 기록 화면 유의어 실시간 표시

현재 유의어·금지어 검사는 InspectSection에서 한 번에 훑는 배치 방식만 있다. 작성 중에
바로 보이게 해 달라는 요청이 있어 검토 중이며, 사용자는 엑셀 형광펜 같은 인라인 방식을
선호한다. **방식은 미결정.** 결정 전에 아래 제약을 반드시 볼 것.

- `RecordSection.vue`의 그리드는 **가상화가 없다.** 학생 × 활동 전 셀을 한 번에 렌더하므로
  DOM의 textarea 수가 두 수의 곱이 된다. 실제 사용자 파일로 확인했을 때 한 학급 규모에서
  이미 상당한 수였다.
- 따라서 셀마다 오버레이 div를 두면 **DOM이 두 배가 되고**, 키 입력마다 정규식
  하이라이트를 돌리게 된다. 비용이 포커스된 셀이 아니라 그리드 전체에 비례한다.
- 셀 textarea는 `bg-transparent`, `overflow-y-auto`, 압축 모드에서 자체 스크롤한다.
  오버레이는 `scrollTop`·줄바꿈·폰트·패딩을 정확히 따라가야 한다.
- 코드베이스에 **IME(composition) 처리가 전무하다**(`compositionstart` 등 0건).
  한글 조합 중 표시가 어긋날 위험이 있다.

**유력안: 포커스된 셀에만 오버레이.** 실시간 피드백이 필요한 곳은 지금 타이핑 중인 셀
하나뿐이다. 나머지 셀은 "금지어 있음" 정도의 가벼운 표시만 지연 계산하면 성능과 IME
문제를 동시에 피한다. 남은 결정은 어떤 유의어 묶음을 실시간 대상으로 삼을지다.
