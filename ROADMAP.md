# 로드맵 · 알려진 제약

이 문서는 **아직 반영되지 않은 개선 항목**과, 사용자가 알아 두면 좋은 현재 동작의
한계를 적어 둡니다. 완료된 변경 내역은 프로그램 안의 릴리즈 노트를 참고하세요.

---

## 사용자가 알아 두면 좋은 것

### 데이터 암호화가 적용되는 범위

암호화를 켜면 다음이 암호화되어 저장됩니다.

- 학생 이름
- 활동 기록 내용
- 기록 히스토리(이전 버전) 내용

반면 **아래 두 가지는 현재 암호화되지 않습니다.**

- 기록 버전을 저장할 때 남기는 **메모**
- 스냅샷을 만들 때 남기는 **메모**

메모는 "무엇을 왜 바꿨는지"를 적는 짧은 칸이라 암호화 대상에서 빠져 있었습니다.
개선 예정이지만, 그때까지는 **메모에 학생 이름이나 민감한 내용을 적지 않는 것을
권합니다.** 예를 들어 "○○ 관련 문구 정리" 대신 "문구 정리" 정도로 적어 주세요.

### 저장이 느린 환경에서는 잠시 멈출 수 있습니다

기록을 저장하는 동안에는 프로그램이 잠깐 반응하지 않습니다. 일반적인 로컬 디스크에서는
저장이 순식간에 끝나 알아채기 어렵지만, **파일을 학교 공유 폴더·네트워크 드라이브·
클라우드 동기화 폴더(OneDrive 등)에 두었거나 백신이 파일을 검사 중이면** 저장이 몇 초
걸릴 수 있고, 그동안 화면이 멈춘 것처럼 보입니다. **그 사이에 친 글자는 입력되지 않습니다.**

가능하면 **파일을 PC 내부 디스크에 두고 사용**하시고, 작업이 끝난 뒤 공유 폴더로
복사하시는 편이 안전합니다.

프로그램이 저장을 처리하는 방식 자체를 바꿔야 해결되는 문제라 예정 작업으로 두었습니다.

### 자동 백업 파일은 자동으로 지워지지 않습니다

파일을 열 때마다 같은 폴더에 `*.db.backup` 파일이 만들어집니다. 프로그램은 이
파일들을 스캔하지도, 지우지도 않습니다. 파일 이름만으로는 프로그램이 만든 것인지
사용자가 보관 중인 것인지 구분할 수 없기 때문입니다. **주기적으로 직접 정리해
주세요.**

암호화를 켜기 전에 만들어진 백업은 암호화되지 않은 사본입니다. 암호화를 켠 뒤에도
그 파일들은 그대로 남아 있으므로 함께 정리하시는 것이 좋습니다.

---

## 예정 작업

### 메모 암호화 (소급 적용 필요)

버전 메모와 스냅샷 메모를 암호화 대상에 넣는 작업입니다.

단순히 "앞으로 저장되는 메모만 암호화"하는 방식은 **채택하지 않기로 했습니다.**
이미 암호화를 사용 중인 파일에는 평문 메모가 들어 있어서, 새 항목만 암호화하면
기존 메모는 영영 평문으로 남기 때문입니다. 따라서 **기존 메모까지 소급해서
암호화하는 절차**가 필요하고, 그 절차를 어떻게 만들지가 정해지지 않아 보류 중입니다.

정해야 할 것:

- 언제 변환할 것인가 — 파일을 열 때 자동으로 / 사용자가 명시적으로 실행
- 변환 도중 실패하면 어떻게 되돌릴 것인가
- 변환 후 파일에 남는 이전 흔적(프리 페이지) 정리를 어떻게 묶을 것인가

### 복호화 실패 기록의 복구 경로

지금은 기록 한 건이라도 복호화되지 않으면 목록 조회 전체가 중단되고, 암호화 해제도
같은 지점에서 막혀 나머지를 건져낼 방법이 없습니다. 어떤 기록이 문제인지도
알려주지 않습니다.

정상적인 사용에서는 발생하지 않고, 파일을 다른 도구로 직접 수정했거나 아주 오래된
버전으로 열어 쓴 경우에 생길 수 있습니다. 다음이 필요합니다.

- 문제가 된 기록을 특정해서 알려주기
- 나머지 기록만이라도 열거나 내보낼 수 있는 경로

### 저장 중 화면 멈춤 해소

기록 저장을 비롯한 대부분의 처리가 화면을 그리는 것과 같은 흐름에서 실행됩니다.
그래서 저장이 오래 걸리면 그동안 화면 전체가 멈추고 그 사이의 키 입력이 사라집니다.

암호화를 켜다가 프로그램이 비정상 종료된 파일을 다시 열 때도 같은 일이 생깁니다. 이때는
남은 정리 작업을 마저 하느라 파일 크기에 비례해 잠시 멈출 수 있습니다. 그 파일을 한 번
열고 나면 다시 발생하지 않습니다.

처리를 별도 흐름으로 옮기면 해결되지만, 데이터베이스 연결을 공유하는 방식까지 함께
손봐야 해서 범위가 큽니다. 저장 자체는 정상적으로 완료되고 데이터가 깨지지는 않으므로,
급한 순서로는 뒤에 두었습니다.

### 암호화 키의 메모리 수명

잠금 해제 후 키는 프로그램을 닫거나 다른 파일을 열 때까지 메모리에 남아 있습니다.
별도의 "잠그기" 기능은 아직 없습니다. 또한 키가 값으로 복사되는 구조라, 메모리에서
지우는 처리가 완전하지 않습니다.

이 항목은 **프로그램이 실행 중인 PC의 메모리를 직접 들여다볼 수 있는 상황**을
전제로 하므로, 파일이 복사·유출되는 상황을 막는 것을 목표로 하는 현재 설계에서는
우선순위가 낮습니다.

---

## 기능 후보

### 기록 화면 유의어 실시간 표시

현재 유의어·금지어 검사는 검사 화면에서 한 번에 훑는 방식만 제공합니다. 기록을
작성하는 중에 바로 표시해 달라는 요청이 있어 검토 중입니다.

방향은 정해지지 않았습니다. 그리드는 학생 × 활동 전체를 한 번에 그리기 때문에 모든
칸에 실시간 검사를 걸면 무거워집니다. **파일을 열 때 전체를 한 번 훑고, 지금
편집 중인 칸만 실시간으로 보는** 방식이 유력하지만, 어떤 유의어 묶음을 실시간
대상으로 삼을지부터 정해야 합니다.
